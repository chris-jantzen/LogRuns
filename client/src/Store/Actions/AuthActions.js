export const signIn = credentials => (dispatch, getState) => {
  // Make mongo call here to authenticate
};

export const signUp = info => (dispatch, getState) => {
  // Make mongo call here to authenticate
};
